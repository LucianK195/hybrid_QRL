"""Dataset-oriented command-line applications.

Applications are intentionally imported from their concrete modules so that a
CartPole command does not eagerly import the heavier dispatch implementations.
"""
